# Backtest Adapter

What backtest adapter look like

# Test case

on the backtest we will have prepare test case first

the test case is like

`SUI_6month.json`

```tsx
[
  {
    vpoint,
    klines1mAfter, // until the next vpoin
    klines5mAfter, // until the next vpoin
  },
];
```

with that kind of test case we can reproduce exactly
