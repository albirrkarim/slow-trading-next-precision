The black swan still using the fetch klines right, i need it using the websocket

see how this project is using websocket


config.minActionableAbsoluteLevel will be config.minEntryAbsLevel

i need also config.maxEntryAbsLevel